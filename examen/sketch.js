let n;
let semilla=154;
let semilla_cuadrado=0;
let numero_interno=0;
let nueva_semilla=0;





function setup() {
  createCanvas(600, 800);
  textSize(40);

}
function draw() {
  fill(255);
  text("deme una semilla de no mas de 3 digitos :",10,10);
 // console.log("deme una semilla de no mas de 3 digitos :")
  //aqui lo voy a pedir el valor de semilla
 // if(semilla.lenght<)
  text(n)
  n++
  semilla_cuadrado= scuare(n);
  text(semilla_cuadrado)
//  console.log(semilla_cuadrado);
 // t
//  numero_interno=
}